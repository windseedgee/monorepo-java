version: 2.1

jobs:
  build:
    docker:
      - image: alpine:3.15
    steps:
      - checkout
      - run:
          name: The First Step for users
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
      - run:
          name: The Second Step for users
          command: |
            ls -al
            echo '^^^The files in your repo^^^'

workflows:
  # Name of workflow
  monorepo-users-build:
    # List of jobs that will run
    jobs:
      - build