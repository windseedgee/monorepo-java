version: 2.1

orbs:
  maven: circleci/maven@1.2.0

jobs:
  build-users:
    docker:
      - image: alpine:3.15
    steps:
      - checkout
      - run:
          name: Build
          command: mvn -B -DskipTests clean package
      - run:
          name: Test
          command: ｜
            cd /users
            mvn test

workflows:
  users:
    jobs:
      - build-users
